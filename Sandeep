from IPython.core.display import display, HTML

html_code = """
<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Talk to Sandeep — Login</title>
  <style>
    body {
      margin:0;
      font-family: Arial, sans-serif;
      height:100vh;
      display:flex;
      justify-content:center;
      align-items:center;
      background: url('https://cdn.pixabay.com/photo/2021/02/11/15/06/talking-6004503_1280.png') no-repeat center center;
      background-size: cover; /* make wallpaper fit screen */
    }

    .overlay {
      position: absolute;
      inset: 0;
      background: rgba(255,255,255,0.7); /* semi-transparent overlay so form is readable */
    }

    .login-box {
      position: relative;
      z-index: 1;
      background: #ffffff;
      border: 1px solid #dbdbdb;
      padding: 40px 35px;
      border-radius: 8px;
      width: 350px;
      text-align: center;
      box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    }

    h2 {
      margin-bottom: 30px;
      font-size: 22px;
      font-weight: bold;
      color: #262626;
      letter-spacing: 2px;
      text-transform: uppercase;
    }

    input {
      width: 100%;
      padding: 12px;
      margin: 8px 0;
      border: 1px solid #dbdbdb;
      border-radius: 4px;
      background: #fafafa;
      font-size: 14px;
      text-transform: uppercase;
    }

    input:focus {
      outline: none;
      border-color: #a1a1a1;
      background: #fff;
    }

    .hint {
      font-size: 12px;
      color: #8e8e8e;
      margin: 10px 0 20px 0;
    }

    button {
      width: 100%;
      padding: 10px;
      background: #0095f6;
      color: #fff;
      font-size: 15px;
      font-weight: bold;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    button:hover {
      background: #0078cc;
    }
  </style>
</head>
<body>
  <div class="overlay"></div>
  <div class="login-box">
    <h2>TALK TO SANDEEP</h2>
    <form onsubmit="alert('Demo login — no backend connected!'); return false;">
      <input type="text" placeholder="NAME (BLOCK LETTERS)" required>
      <input type="password" placeholder="PASSWORD (BLOCK LETTERS)" required>
      <p class="hint">Hint: What relation you'll be with Sandeep</p>
      <button type="submit">Login</button>
    </form>
  </div>
</body>
</html>
"""

display(HTML(html_code))
